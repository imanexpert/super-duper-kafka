#!/usr/bin/env bash

# Install all the dependencies
bundle install
bundle add karafka
bundle exec karafka install
yarn install
bundle install karafka
# Create db
bundle exec rake db:create
bundle exec rails db:migrate
# Create test db
bundle exec rake db:test:prepare
# Seed dev db
bundle exec rails db:seed

